#include<stdio.h>
post_eval(char post_exp[])
{
for(int i=0;pos_exp[i]!='\0';i++)
{
if(isdigit(post_exp[i]))
push(post_exp[i]='\0');
else
{
a=pop();
b=pop();
switch(c)
{
case'+':result=a+b;
case'-':result=a-b;
case'*':result=a*b;
case'%':result=a/b;
}
push[result];
}
push[int]{ }
pop() { }
void main()
{
int,stack[MAX];
char post=exp[20];
}

